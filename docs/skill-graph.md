# Skill Dependency Graph

> **Pending full regen.** This map is generated by the `capabilities-map` skill (graph view: `var: graph`), which parses every `SKILL.md` for `depends_on` / `consume` / reactive edges. The **203→102 skill consolidation** (merges + deletes) invalidated the previous 176-node diagram — its per-category mermaid graphs and dependency edges are regenerated on the next scheduled `capabilities-map graph` run, not maintained by hand. The accurate current **inventory** is below; the authoritative catalog is [`skills.json`](../skills.json) + [`packs.json`](../packs.json) and the README pack tables.

**102 skills across 10 packs.**

## Inventory by pack

| Pack | Key | Skills |
|------|-----|--------|
| **Core** | `core` | 12 |
| **Fleet & Replication** | `fleet` | 5 |
| **Research & Content** | `research` | 16 |
| **Dev & Code** | `dev` | 18 |
| **Crypto & Markets** | `markets` | 16 |
| **Onchain Security** | `hound` | 4 |
| **Social & Writing** | `social` | 8 |
| **Productivity** | `productivity` | 11 |
| **Agent Ops** | `agent-ops` | 12 |
| **Lab** | `lab` | 0 |

## Edge legend (for the regenerated graph)

| Edge | Meaning |
|------|---------|
| `-->` solid | `depends_on` — skill requires another to run first |
| `-.->` dashed | `consume` — chain step receives output from a prior step |
| `-..->` dotted | reactive trigger or shared-state dependency |

Every skill also writes `memory/cron-state.json`, read by the self-healing loop (`heartbeat`, `skill-health`, `skill-repair`, `skill-evals`, `self-improve`) — collapsed into this callout rather than drawn as one edge per skill.

## Currently enabled (live in `aeon.yml`)

`heartbeat`

---

_To regenerate the full dependency diagram: run the `capabilities-map` skill with `var: graph` (writes this file), or browse the dashboard **Packs** view for the interactive catalog._
